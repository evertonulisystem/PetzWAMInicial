plugins {
    id 'java'
}
group 'br.com.gotasdetecnologia'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'junit:junit:4.13.2'
    testImplementation group: 'junit', name: 'junit', version: '4.13.2'
    testImplementation 'info.cukes:cucumber-junit:1.2.6'
    implementation group: 'info.cukes', name: 'cucumber-java', version: '1.2.5'
    implementation group: 'org.seleniumhq.selenium', name: 'selenium-java', version: '3.141.59'
    testImplementation group: 'info.cukes', name: 'cucumber-picocontainer', version: '1.2.5'

    // gera o json e o html com os  dados do teste para o relat associado as features e cenario gherkin
   implementation group: 'com.aventstack', name: 'extentreports', version: '3.0.7'//5.0.1 nao vai funioanrar

    // gera o dashboard apartir do aventstack
    implementation 'com.vimalselvam:cucumber-extentsreport:3.1.1'
    implementation group: 'org.freemarker', name: 'freemarker', version: '2.3.28'
}

test{
    useJUnit()
}

task regressionTests(type: Test){ //T maiusculo
    include '**/Runner*'
}

task smokeTest(type: Test) {
    include '**/Runner*'
    exclude '**/RunnerBackup*'
}